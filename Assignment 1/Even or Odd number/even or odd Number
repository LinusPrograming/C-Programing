#include <stdio.h>
int main()
{
    int ivalue=0;

    printf("Enter a number :");
    scanf("%d",&ivalue);

    Chknum(ivalue);

    return 0;
}
void Chknum(int number)
{
    if (number%2==0)
    {
        printf("EVEN number");
    }
    else
    {
        printf("ODD number");
    }

}
